[project]
name = "cybai"
version = "0.1.0"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.black]
line-length = 88

[tool.flake8]
max-line-length = 88
exclude = ["venv", ".git", "__pycache__"]
